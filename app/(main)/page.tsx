/**
 * Feed page — home route (/)
 * Placeholder for Phase 3. Will become the main discovery feed with
 * New/Popular tabs, category filters, and infinite scroll.
 */
export default function FeedPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] text-center">
      <h2 className="text-2xl font-semibold text-gray-900 mb-2">Feed</h2>
      <p className="text-gray-500 text-sm">Coming in Phase 3 — works, creators, and discovery.</p>
    </div>
  )
}
